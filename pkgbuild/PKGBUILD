# Maintainer: Bruno Goncalves <bigbruno@gmail.com>

pkgname=biglinux-grub-restore
pkgver=4.0.0
pkgrel=1
arch=('x86_64')
license=('GPL-2.0-or-later')
url="https://github.com/biglinux/biglinux-grub-restore"
pkgdesc="Restore your installed system"
depends=('btrfs-progs' 'grub' 'gtk4' 'libadwaita' 'manjaro-tools-base' 'mkinitcpio' 'os-prober' 'pacman' 'pamac-cli' 'polkit' 'python' 'python-gobject' 'util-linux' 'vte4')
source=("git+https://github.com/biglinux/biglinux-grub-restore.git")
md5sums=(SKIP)

package() {
    # Verify default folder
    if [ -d "${srcdir}/${pkgname}/${pkgname}" ]; then
        InternalDir="${srcdir}/${pkgname}/${pkgname}"
    else
        InternalDir="${srcdir}/${pkgname}"
    fi


    # Copy files
    if [ -d "${InternalDir}/usr" ]; then
        cp -r "${InternalDir}/usr" "${pkgdir}/"
    fi

    if [ -d "${InternalDir}/etc" ]; then
        cp -r "${InternalDir}/etc" "${pkgdir}/"
    fi

    if [ -d "${InternalDir}/opt" ]; then
        cp -r "${InternalDir}/opt" "${pkgdir}/"
    fi

    install -Dm644 "${InternalDir}/LICENSE" "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
}
